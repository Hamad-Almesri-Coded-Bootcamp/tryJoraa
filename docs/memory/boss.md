# Memory — boss

One line per decision, dated, never deleted. `CLAUDE.md` → Memory.

- 2026-09-19 · D21 adopted: the frame is patient-side aggregation across clinics and sectors, not a single ministry system. Supersedes D3 and D7; the ministry deployment stays as pitch and tone, never as chrome.
- 2026-09-19 · D22 adopted: seven agents on the architecture, four running Thursday (orchestrator, rescheduling, interaction screening, profiler), extraction a Tuesday stretch, adherence outreach and travel check roadmap. A roadmap agent never appears in the UI as a dead button.
- 2026-09-19 · D23 adopted: no agent computes a number. All dose arithmetic, catch-up windows, interaction distances and depletion forecasts run in Code nodes or SQL. Written into CLAUDE.md non-negotiables and guardrail G11. This was the vision's central safety rule and the contract had left it unwritten.
- 2026-09-19 · D24 adopted: nine tables. Pharmacist fields, source/sector, and route/indication/generic-vs-brand all land in Saturday's migration rather than a second migration mid-week.
- 2026-09-19 · D25 adopted: the "readable by exactly two people" rule is replaced — a prescription is readable by its patient and any doctor linked through doctor_patients; doctor_id is nullable. verify-rls.ts gains four assertions it did not have, including that patient B cannot insert with patient_id = A.
- 2026-09-19 · D26 adopted: extraction writes to prescription_drafts, never to prescriptions. G8 is scoped, not weakened. Third instance of the same draft-then-human-accepts shape, after D11a and D15.
- 2026-09-19 · D27 adopted: Thursday's grounded corpus is our own seeded interactions and medications tables with per-row sources; RxNorm is roadmap and DrugBank licensing is not a five-day problem. A doctor verifying a drafted interaction writes it into `interactions`, so G4 stays literally true.
- 2026-09-19 · D28 adopted: depletion forecasting ships as a SQL view, not an agent. In-app refill requests are roadmap; the routing rule (public to public, private to private, capped at the authorised duration) is recorded now so it is not invented later.
- 2026-09-19 · D29 adopted: fourteen routes. /prescriptions/add and the full aggregated list on /doctor/patients/[id] are exempt from the Tuesday cut order — they are what make D21 real rather than claimed.
- 2026-09-19 · PROMPT-0.md marked as a record, not an instruction, with an AMENDMENTS section restating steps 2–5 and additions to 8, 9 and 10. The original prompt text is preserved verbatim.
- 2026-09-19 · docs/technical-plan.md written — schema SQL, RLS policies, n8n topology, the deterministic layer, cross-lane handoffs. It is the how; PRODUCT-DECISIONS.md stays the why.
- 2026-09-19 · Correction: verify-rls.ts needs NO service role key, not even in setup. The script on disk was already anon-only with an assertNotPrivileged guard; PROMPT 0 step 5 was wrong and the earlier amendment repeated its error. Only seed-auth.ts touches the key.
- 2026-09-19 · Correction: two column vocabularies existed. The migration follows D24 (drug_name_generic, dose_per_administration, frequency_per_day, dosing_pattern) and the TABLES block in verify-rls.ts is rewritten to match. `strength` splits into strength_value numeric + strength_unit enum, because '500mg' as text breaks "a number is a number".
- 2026-09-19 · Documents polished into one consistent set. PROMPT-0.md rewritten as a single current bootstrap (amendments folded in, header no longer claims it has run — nothing is built yet). PRODUCT-DECISIONS.md keeps every D-number; D3, D7, D11, D20 are stubs pointing at their successors.
- 2026-09-19 · Contradiction fixed: CLAUDE.md said the service role key may be used in verify-rls.ts setup and teardown; the technical plan and the script say nowhere. CLAUDE.md now says nowhere. The key is also removed from CI — neither build nor verify:rls needs it, and CI greps verify-rls.ts for it.
- 2026-09-19 · D30 adopted: an append-only `audit_log` table, tenth table, written by one security-definer trigger function and readable like a prescription. Patient sees it at /history, linked doctor on /doctor/patients/[id]. Probe 7 added to verify-rls.ts. Lane A owns the table, B the screens.
- 2026-09-19 · D31 adopted: web app first, native later. Consequence now: logic in Postgres/n8n/api routes only, routes take cookie or bearer auth and are listed in docs/api.md, strings in src/i18n/. Nothing native is built this week.
- 2026-09-19 · Pharmacist fields sit on `prescriptions`, not a child table (was open in D24). One dispense per prescription is enough this week.
- 2026-09-19 · SE-6 process fixed: the boss runs the gstack /cso skill on main Monday evening and Wednesday before the freeze; report in docs/security-audit.md (boss-only); fixes become lane tasks.
- 2026-09-19 · Plan and workflow changes now have a fixed path (CLAUDE.md "Changing the plan or the workflow"): lanes edit their own plan; workflow changes are proposed under `## Workflow proposals` in the plan file and applied by /ship step 5b.
- 2026-09-19 · .gitignore added at the root: .env*, .gstack/ (holds a session token), docs/evidence/, .logs/. The repo is public and /work runs `git add -A`, so this was a leak waiting to happen.
- 2026-09-19 · Exactly three security-definer functions are allowed in the schema: audit_row(), audit_reference_row(), handle_new_user(). Reference-table audit rows take the run's patient when an agent wrote them, else the acting doctor; seed rows with neither are skipped. The reviewer and integrator flag any fourth.
- 2026-09-19 · Repo prepared for its first public push to main: .gitignore, README.md front page (names for A, B, C left for the team to fill), docs/playbook.html (the team guide with master prompts, boss-owned), trailing whitespace stripped, secret scan clean, pre-commit hook dry-run clean. Nothing committed by the session; the lead commits.
- 2026-09-19 · Pre-commit hook had two bugs found by dry-running it over the whole tree: the private-key pattern began with dashes so grep read it as an option (that check never ran), and the webhook-test warning fired on every doc that mentions the URL to forbid it. Fixed: `grep -e`, and the warning is limited to code and config paths. Self-tested with a fake JWT, a fake private key and a fake test URL.
